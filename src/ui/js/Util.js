.pragma library

function max(a, b) {
    var max;
    if (a > b) max = a;
    else max = b;

    return max;
}

function min(a, b) {
    var min;
    if (a < b) min = a;
    else min = b;

    return min;
}
